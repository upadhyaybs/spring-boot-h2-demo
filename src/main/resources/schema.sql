CREATE TABLE restaurant (
  id         LONG IDENTITY,
  name 		 VARCHAR(30),
  address	 VARCHAR (200),
  city       VARCHAR (200),
  state      VARCHAR (20),
  zipCode    VARCHAR (10),
  phone 	 VARCHAR(15),
  email      VARCHAR (50)
);